version: "3.8"

services:
  fdi:
    image: africahazardswatch/fdi:latest
    container_name: fdi
    build: .
    restart: "no"
    command: tail -f /dev/null
    volumes:
      - ${DATA_VOLUME}:/data